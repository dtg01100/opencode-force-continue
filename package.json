{
  "name": "opencode-force-continue",
  "version": "1.0.0",
  "type": "module",
  "main": "force-continue.server.js",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.2"
  }
}
